import 'package:flutter/material.dart';
import 'package:sayaaratukum/util/constant.dart';

class HorizontalSpace4 extends StatelessWidget {
  const HorizontalSpace4({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      width: Constants.spacingSmall,
    );
  }
}

class HorizontalSpace8 extends StatelessWidget {
  const HorizontalSpace8({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      width: Constants.spacingXSmall,
    );
  }
}

class HorizontalSpace12 extends StatelessWidget {
  const HorizontalSpace12({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      width: Constants.spacingMedium,
    );
  }
}

class HorizontalSpace16 extends StatelessWidget {
  const HorizontalSpace16({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      width: Constants.spacingXXSmall,
    );
  }
}

class HorizontalSpace20 extends StatelessWidget {
  const HorizontalSpace20({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      width: Constants.spacingXMedium,
    );
  }
}
